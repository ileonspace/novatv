# NovaTV 合规与封号风险分析

> 记录 NovaTV 与原项目 LunaTV 的对比、Cloudflare 封号风险分析、合规定位。
> 创建日期：2026-08-04 ｜ 本文档保持客观，只陈述事实与风险，不做价值判断。

---

## 一、核心结论

**NovaTV 的定位：一个自用、合规、私密的影视搜索工具（空壳框架）。**

- 无内置影视源，源配置由用户自行导入，**仅存于本地浏览器 localStorage**
- 服务端**零存储**：不存视频、不存用户数据、不存源配置
- 播放直连源站，**不占服务器流量**
- 单密码访问，无多用户体系

---

## 二、与原项目 LunaTV 的对比

### 1. 技术架构差异

| 维度 | LunaTV（原项目）| NovaTV（精简版）|
|---|---|---|
| 数据库 | 有（upstash/redis/kvrocks，可选）| **无**（空实现，LocalMemoryStorage）|
| 数据存储 | 默认 localStorage，可选外部 DB | **纯浏览器 localStorage/IndexedDB** |
| 用户体系 | 注册 + 角色 + 封禁 | **单密码**，无注册 |
| 部署方式 | 仅 Docker / Zeabur / Vercel | **Cloudflare Worker**（opennext）|
| 源配置 | 可配数据库/服务端 | **仅用户本地导入** |

### 2. 关键事实：数据都不在 Cloudflare

- **LunaTV 数据本就不存 Cloudflare**：默认 localStorage，可选连外部 Redis/Upstash（第三方服务，非 CF）
- **NovaTV 数据同样不存 Cloudflare**：纯浏览器存储
- 两者的差异在于：LunaTV **能选**连数据库，NovaTV **彻底移除**了数据库能力

### 3. 部署目标差异

- **LunaTV**：官方仅支持 Docker/Zeabur，**未做 Cloudflare 适配**（无 wrangler 配置、0 处 edge runtime）
- **NovaTV**：专门为 Cloudflare 适配（opennext 迁移 + Worker 部署）

---

## 三、Cloudflare 封号风险分析

### 1. LunaTV 被封的可能原因（客观判断）

最可能的原因：**聚合影视源**（若部署时配置了未授权源，CF 能看到它聚合影视内容，收到版权投诉 DMCA 后封号）。技术架构（数据库/部署方式）**不是**封号主因。

### 2. NovaTV 的风险评估

| 维度 | LunaTV（被封的）| NovaTV（现在）|
|---|---|---|
| 服务器上有源吗 | 可能配了源 | **无内置源（空壳）** |
| 源存在哪 | 可配数据库/服务端 | **用户本地浏览器** |
| CF 能看到什么 | 影视聚合内容 | **功能框架**（无影视内容）|
| 用户数据 | 有数据库 | **零存储** |
| 账号 | 未知 | **非主账号**（规避账号历史）|

**结论：NovaTV 对 Cloudflare 来说是"干净空壳"，比 LunaTV 更难被 CF 以"托管影视内容"为由封号。**

### 3. 仍存在的风险（诚实提醒）

1. **代码能力可推断**：CF 若收到投诉审查代码，仍能看出这是"影视搜索/聚合框架"（功能性质在代码里）
2. **账号历史因素**：非主账号已规避，但若同一非主账号有过封号记录仍需注意
3. **根本的版权风险**：只要应用是影视聚合性质（哪怕空壳），一旦导入未授权源并被投诉，风险就在——**风险从"部署时内容"转移到了"使用时的源"**

---

## 四、合规定位与使用原则

**项目宗旨：保护正版，人人有责。**

1. **空壳自用**：NovaTV 部署到 CF 时无任何源，对 CF 是空白框架
2. **源由用户导入**：源仅存用户浏览器，不进服务器
3. **只自用不开放**：不公开传播、不提供给别人使用，避免被用于盗版传播
4. **导正规源**：如需使用，优先导入有授权的源，降低版权风险
5. **非主账号部署**：绝不用主账号 vipleon@gmail.com（已因封号风险禁用）

---

## 五、相关文档

- `PROJECT.md` — 项目架构说明
- `ITERATION_LOG.md` — 迭代日志
- `CHANGES_v1.2.9_20260803.md` — v1.2.9 系列改动总结
- `README.md` — 项目使用说明
